(function (module, require, window) {
  'use strict';

  var ApiError = require('../exceptions/apiError.js');

  // loginController
  module.exports = ['$scope', '$http', 'User',
    function userPreferencesController($scope, $http, User) {
    }];

})(module, require, window);
